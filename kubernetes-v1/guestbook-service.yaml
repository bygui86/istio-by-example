kind: Service
apiVersion: v1
metadata:
  name: guestbook-service
  labels:
    app: guestbook-service
    visualize: "true"
spec:
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: guestbook-service
    serving: "true"
